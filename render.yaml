services:
  - type: web
    name: jhenkar-portfolio
    runtime: static
    buildCommand: echo "No build required for static portfolio"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
